import { NextResponse } from "next/server";
import { requireAdmin } from "@/lib/auth";
import { getSupabaseAdminClient } from "@/lib/supabase-admin";
export async function POST(_request: Request, context: { params: Promise<{ id: string }> }) { const auth = await requireAdmin(); if ("error" in auth) return auth.error; const { id } = await context.params; const admin = getSupabaseAdminClient(); const guarantee = await admin.from("postpaid_guarantees").select("id").eq("company_id", auth.companyId).eq("id", id).maybeSingle(); if (!guarantee.data) return NextResponse.json({ error: "Garantia não encontrada." }, { status: 404 }); const { data, error } = await admin.rpc("reverse_guarantee_payment", { p_guarantee_id: id, p_admin_id: auth.user.id }); if (error) return NextResponse.json({ error: "Não foi possível estornar o pagamento." }, { status: 409 }); return NextResponse.json({ result: data }); }
